- var itemID = productContent.uid
- var itemName = productContent.getText('product.name') ? productContent.getText('product.name') : "Unknown"
- var itemImageURL = image ? image.url : null
- var itemWeight = productContent.getNumber('product.snipcartWeight')
- var itemDescription = productContent.getText('product.snipcartDescription')
- var itemMaxQuantity = productContent.getNumber('product.snipcartMaxQuantity')
- var itemURL = pageUrl

a.button.add-to-cart.snipcart-add-item(
    href="#" data-item-id=itemID
    data-item-name=itemName
    data-item-price=price
    data-item-image=itemImageURL
    data-item-weight=itemWeight
    data-item-url=itemURL
    data-item-description=itemDescription
    data-item-max-quantity=itemMaxQuantity)
    | Add To Cart
