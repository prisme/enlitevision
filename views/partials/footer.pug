if layoutContent != null
  footer.Footer.clearfix.py4
    div.md-flex.justify-between
      each item, index in layoutContent.getGroup('layout.footerColumns').toArray()
        .Footer-col.m2
          != item.getStructuredText("column").asHtml(ctx.linkResolver)

    .Footer-logo
      img(src= layoutContent.getImage('layout.footerLogo').url, alt='enlite vision' )
